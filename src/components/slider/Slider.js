import classes from "./Slider.module.css";

const Slider = () => {
  return (
    <div className={classes["slider-container"]}>
      <h1>Place for slider</h1>
    </div>
  );
};

export default Slider;
